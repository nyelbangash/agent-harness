# unrelated pre-existing file, kept on disk but out of the repo
/dashboard.html

# personal / machine-specific — kept on disk, not in the public repo
# (ops/policy.example.yaml is the committed template)
/ops/policy.yaml
/.claude/
/PROGRESS.md
/HANDOFF-agentic-harness.md

# per-run git worktrees
/workspaces/*
!/workspaces/.gitkeep

# macOS
.DS_Store

# databases (belt and braces; real DBs live in ~/.harness)
*.db
*.db-*

# Phoenix / Elixir
/harness/_build/
/harness/deps/
/harness/.elixir_ls/
/harness/.lexical/
/harness/priv/static/assets/
/harness/assets/node_modules/
harness-*.tar
erl_crash.dump
/harness/tmp/
